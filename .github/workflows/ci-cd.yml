name: CI for Drogon App

on:
  push:
    branches: [master]

env:
  IMAGE_NAME: littelrepka/hello-drogon3.0

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v3

    - name: 🐳 Build Docker image
      run: docker build --no-cache --progress=plain -t $IMAGE_NAME .

    - name: ✅ Run tests
<<<<<<< HEAD
      run: docker run --rm $IMAGE_NAME ctest --test-dir /app/build --output-on-failure
=======
      run: docker run --rm $IMAGE_NAME ctest --test-dir /app/build --output-on-failure
>>>>>>> 748910eeb6dd87d3a11eafcc37d6673c1dce9604
